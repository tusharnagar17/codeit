export interface ServicesProps {
  icon: JSX.Element;
  name: String;
  detail: String;
}

export interface ContactFormPros {
  name: string;
  email: string;
  phone: string;
  budget: string;
  description: string;
}
export interface projectDetailsProps {
  name: string;
  description: string;
  techStack: string[];
  imageLinks: string;
}

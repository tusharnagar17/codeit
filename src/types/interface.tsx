export interface ServicesProps {
  icon: JSX.element;
  name: String;
  detail: String;
}

export interface ContactFormPros {
  name: string;
  email: string;
  phone: string;
  budget: string;
  description: string;
}
